# This is a config for E2B sandbox template.
# You can use template ID (k8u8angsjc2zufymeywl) or template name (v0-clone) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("v0-clone") # Sync sandbox
# sandbox = await AsyncSandbox.create("v0-clone") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('v0-clone')

team_id = "1928971c-19c9-45d9-b28e-0b478c0a636d"
start_cmd = "/compile_page.sh"
dockerfile = "e2b.Dockerfile"
template_name = "v0-clone"
template_id = "k8u8angsjc2zufymeywl"
